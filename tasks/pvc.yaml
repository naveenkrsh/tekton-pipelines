apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-pv-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  volumeName: tekton-pv
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
